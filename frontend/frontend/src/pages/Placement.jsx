import React from 'react';
import { Typography, Box } from '@mui/material';

const Placement = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Placement & Career
      </Typography>
      <Typography variant="body1">
        Manage job postings and student placements.
      </Typography>
    </Box>
  );
};

export default Placement;