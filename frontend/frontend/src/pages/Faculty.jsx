import React from 'react';
import { Typography, Box } from '@mui/material';

const Faculty = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Faculty Management
      </Typography>
      <Typography variant="body1">
        Manage faculty information, assignments, and profiles.
      </Typography>
    </Box>
  );
};

export default Faculty;